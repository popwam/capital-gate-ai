AICG FINAL AI + UI/UX PACKAGE

This package combines:
- Full Admin UI/UX update
- Master Plan spatial editor changes
- Payment plan designer/calculator changes
- Customer AI response + streaming fixes
- Groq multi-model routing/fallback code
- Latest Admin Leads JSX build fix

Replace/copy the package contents over the repository root.

Then run from repository root:
1) npm run db:generate
2) npx prisma migrate deploy --schema packages/database/prisma/schema.prisma
3) npm run build -w @maqar/api
4) npm run build -w @maqar/web

Important:
- Review Railway/production env vars before deployment.
- Do not use prisma db push in production.
- If a migration is already applied, migrate deploy will safely report no pending migration.
